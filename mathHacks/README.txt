Basically, tell it what you want to calculate, ex 'cone', 'cube', 'MAD', and 'interest' (then say either 'compound' or 'simple') 
Then give it the values.
For MAD, give it the values in the list, then do a double space to request to end
Note that I am adding more as I go through math class
